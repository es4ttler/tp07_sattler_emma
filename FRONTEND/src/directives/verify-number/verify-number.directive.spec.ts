import { VerifyNumberDirective } from './verify-number.directive';

describe('VerifyNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new VerifyNumberDirective();
    expect(directive).toBeTruthy();
  });
});
