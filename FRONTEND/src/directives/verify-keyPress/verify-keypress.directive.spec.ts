import { VerifyKeypressDirective } from './verify-keypress.directive';

describe('VerifyKeypressDirective', () => {
  it('should create an instance', () => {
    const directive = new VerifyKeypressDirective();
    expect(directive).toBeTruthy();
  });
});
